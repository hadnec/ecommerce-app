import React from 'react';
import { Box, Image } from '@chakra-ui/react';

const Logo: React.FC = () => {
  return (
    <Box>
      <Image src="/path/to/logo.png" alt="Logo" />
    </Box>
  );
};

export default Logo;
